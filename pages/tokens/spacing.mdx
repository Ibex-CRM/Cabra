import * as tokens from '../../npm/styles/js/variables.js'

# Spacing

{console.log(tokens)}

<table style={{ minWidth: '50vw', marginTop: '30px' }}>
  <thead>
    <tr><th>Name</th><th>Size</th><th>-</th></tr>
  </thead>
  <tbody>
    {Object.entries(tokens['Core'].space).map(([key, space]) => {
      return (
        <tr key={`${key}`}>
          <td style={{ margin: '20px 0px 0px 0px', color: 'black' }}>{key}</td>
          <td style={{ display: 'flex', flexWrap: 'wrap' }}>{space.value}</td>
          <td>
            <div
              style={{
                height: '15px',
                width: space.value,
                backgroundColor: 'darkgrey',
              }}
            ></div>
          </td>
        </tr>
      )
    })}
  </tbody>
</table>
