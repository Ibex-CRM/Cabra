import * as tokens from '../../../npm/styles/js/variables.js'

# Border Width
<hr style={{margin: '0px', filter: 'contrast(600%) invert(0.7)', height: '2px' }}/>

<div style={{display: 'flex', gap: '2vw', flexWrap: 'wrap'}}>
  {Object.entries(tokens['Core'].borderWidth).map(([borderWidthName, borderWidth]) => {
    return (
      <div style={{display: 'flex', flexDirection: 'column'}}>
        <p>{borderWidthName}</p>
        <div style={{width:  '8vw', minHeight: '8vw', border: borderWidth.value + " solid " + tokens['Core'].colors.primary.goat.value}}/>
      </div>
    )
  })}
</div>
